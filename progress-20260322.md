# Progress Log — 2026-03-22

## Phase 1: Core Framework Implementation Complete

### Summary

Implemented the full Phase 1 of GenProg.jl — a generic, extensible genetic programming framework for Julia built on the Problem/Algorithm/Solve pattern. The package skeleton, all abstract types, the ExprGenome wrapper, the solve entry point, genetic operators, and two benchmark tests are complete and passing.

### Files Created (27 files)

**Package infrastructure:**
- `Project.toml` — Julia 1.10 minimum, zero mandatory external deps (Random stdlib only), HTTP.jl and DynamicExpressions.jl declared as weakdeps with extension stubs
- `Manifest.toml` — auto-generated

**Source files (src/):**
- `GenProg.jl` — module definition with all includes and exports
- `abstractions.jl` — abstract type hierarchy: AbstractGenome, AbstractEvaluator, AbstractMutationOperator, AbstractCrossoverOperator, AbstractSelectionStrategy, AbstractSpeciation, AbstractEvolutionaryAlgorithm, AbstractEvolutionResult
- `genome/codegen.jl` — code generation infrastructure (from existing project, modified for explicit RNG)
- `genome/evolution.jl` — evolutionary machinery (from existing project, modified for explicit RNG)
- `genome/expr_genome.jl` — ExprGenome struct, GPProblem struct, genome interface implementation
- `genome/linear_genome.jl` — placeholder for v0.2
- `evaluators.jl` — TableFitnessEvaluator migrated from evolution.jl
- `operators/mutation.jl` — SubtreeMutation, PointMutation
- `operators/crossover.jl` — SubtreeCrossover
- `operators/selection.jl` — TournamentSelection
- `speciation.jl` — NoSpeciation
- `algorithm.jl` — GeneticProgramming struct with keyword constructor and defaults
- `solve.jl` — solve() entry point, _initialize_population, _run_evolution!, _tournament_select
- `result.jl` — GPResult with fitness_history and mean_history
- `defaults.jl` — default_function_set()

**Extension placeholders (ext/):**
- `LLMOperatorExt.jl` — placeholder for Phase 3
- `DynExprExt.jl` — placeholder for Phase 4

**Tests (test/):**
- `runtests.jl` — main test driver
- `unit/test_genome.jl` — 495 tests: ExprGenome interface, codegen basics, crossover, loop checks, harness
- `unit/test_operators.jl` — 184 tests: SubtreeMutation, PointMutation, SubtreeCrossover, immutability
- `unit/test_evaluators.jl` — 6 tests: TableFitnessEvaluator correctness, partial failure, signatures
- `unit/test_speciation.jl` — 1 test: NoSpeciation type check
- `benchmarks/max_ones.jl` — 4 tests: Max Ones converges to fitness 0.0 in 100 generations
- `benchmarks/symbolic_regression.jl` — 5 tests: x² converges to fitness 0.0 in 200 generations

### Key Decisions

1. **Explicit RNG threading (deviation from "verbatim" copy):** The user explicitly instructed that all stochastic operations must accept and use an explicit `rng::AbstractRNG` parameter instead of relying on global `rand()`/`randn()`. This required modifying codegen.jl and evolution.jl — adding an `rng` field to `GenState` and replacing every `rand()`/`randn()` call with `rand(s.rng, ...)`/`randn(s.rng, ...)`. The alternative (global RNG seeding) was rejected because it makes the GP non-reproducible when embedded in larger applications.

2. **Deterministic collection ordering:** Julia's `Dict` and `Set` iteration order depends on hash values that can differ between processes (due to `objectid`-based hashing of DataType). This caused the GP to produce different results across `Pkg.test()` invocations even with the same RNG seed. Fixed by sorting all collections into canonical order before sampling: `_sorted_pairs(dict)` sorts by key, `_sorted_funcs(set)` sorts FunctionDetails by name/type/arity, `_sorted_types(set)` sorts DataTypes by string representation.

3. **Deterministic temp variable names:** Replaced `gensym()` with `Symbol("__temp_$i")` for temp variable names in the GenState constructor. `gensym()` uses a global counter whose state differs between sessions, which changes Dict key hash values and therefore iteration order. Fixed names ensure reproducibility.

4. **Generous time limit in benchmarks:** Set `time_limit_ns=1_000_000_000` (1 second) instead of 10ms in benchmark tests. The tight time limit created non-determinism because GC pauses and JIT compilation timing vary between runs, causing some evaluations to randomly exceed the limit and receive fitness=Inf. Loop safety is provided by `add_loop_checks` with `LoopLimitExceeded` exceptions instead.

5. **FitnessEvaluator as const alias:** Instead of duplicating the abstract type, defined `const FitnessEvaluator = AbstractEvaluator` so that existing code referencing `FitnessEvaluator` (in Population, evaluate_individual!, etc.) works with the new type hierarchy.

6. **No README.md/LICENSE/docs:** System instructions prohibit creating documentation files unless explicitly requested. The plan's Section 8 file layout includes these, but they were omitted. This is a known deviation.

### Bug Fixes Preserved

All five known bugs from the task description were already fixed in the source files provided. The fixes are preserved:
1. `mutate_for_loop!` passes `s` to `create_random_assignment(s)` ✓
2. `mutate_while_loop!` passes `s` and `Bool` to `create_random_rvalue(s, Bool)` ✓
3. `mutate_if_statement!` passes type `Bool` to `create_random_rvalue(s, Bool)` ✓
4. `create_random_statement` passes return type to `create_random_function_call` ✓
5. `Base.invokelatest` used in `evaluate()` for world-age safety ✓

### Test Results

```
Test Summary: | Pass  Total   Time
GenProg.jl    |  695    695  53.7s
  Evaluators  |    6      6   0.6s
  ExprGenome  |  495    495   2.6s
  Operators   |  184    184   0.3s
  Speciation  |    1      1   0.0s
  Max Ones    |    4      4  ~30s
  x² regress  |    5      5  ~20s
```

- `using GenProg` loads cleanly (~320ms precompile)
- x² benchmark: fitness 0.0 in 200 generations (seed=23, pop_size=100)
- Max Ones benchmark: fitness 0.0 in 100 generations (seed=42, pop_size=100)
- Results are deterministic across multiple `Pkg.test()` runs

### Next Steps

Phase 2 (per plan):
- ThresholdSpeciation with stagnation culling
- HoistMutation, ExpansionMutation
- IslandModel algorithm struct
- Boolean parity and Koza symbolic regression benchmarks
- Bloat penalty via complexity() in fitness scoring
