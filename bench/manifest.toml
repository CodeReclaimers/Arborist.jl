# Arborist benchstone manifest.
#
# Companion to /home/alan/benchstone (the benchmark harness).
# Registered projects are invoked via the `invocation` template; each
# [[benchmarks]] entry declares a single entry point implemented in
# bench/benchmarks.jl. See bench/README.md for the contract summary.

[project]
name = "Arborist"
language = "julia"
# `--threads=8` is baked into the template so the subprocess gets the
# declared thread count regardless of caller environment. Matches each
# benchmark's `threads = 8` declaration below.
invocation = "julia --project=. --threads=8 bench/benchmarks.jl --entry={entry_point} --config={config_path} --output={output_path}"

# ---------------------------------------------------------------------------
# Primary benchmark: NSGA-II bin packing, metric = test-seed fitness of
# best Pareto member. Matches the 2026-04-18 Karpathy-loop setup.
# ---------------------------------------------------------------------------
[[benchmarks]]
name = "nsga2_binpack_mean_fitness"
entry_point = "nsga2_binpack_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 1080  # ~18 min/rep; 90 min total for 5 reps
threads = 8
gpu = "none"
background_required = true

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/binpack_common"
# sha256 of bench/corpus/binpack_common/corpus_spec.toml — the only file in
# the corpus. See bench/README.md for the hashing convention.
corpus_hash = "sha256:ece0e56a31e599434b190ce69829bb833f07383d0678d297596bf243ff84d2d3"

# ---------------------------------------------------------------------------
# Secondary benchmark: Koza-1 symbolic regression on TreeGenome. Picked as
# the contract-stress case: different genome type, different algorithm,
# different objective shape (single-objective MSE), different RNG
# discipline, deterministic bytewise corpus, short runtime.
# ---------------------------------------------------------------------------
[[benchmarks]]
name = "koza_regression_mean_fitness"
entry_point = "koza_regression_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 10
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/koza1"
corpus_hash = "sha256:7ed5465cba7593c18bba0e852133c50f1e8602e44d701a15bb09f7cd88411feb"

# ---------------------------------------------------------------------------
# Phase A neuroevolution benchmarks: GraphGenome / NEAT on classification.
# Added 2026-04-21. See test/benchmarks/{parity,two_spirals*}_neat.jl for the
# corresponding test-suite checks and convergence thresholds.
# ---------------------------------------------------------------------------

[[benchmarks]]
name = "parity3_mean_fitness"
entry_point = "parity3_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 10
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/parity3"
corpus_hash = "sha256:7139615b5f31a01be6a241f4c8997dfd75069cf0366f41d9e31cb862f22f76c4"

[[benchmarks]]
name = "two_spirals_mean_fitness"
entry_point = "two_spirals_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 30
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/two_spirals"
corpus_hash = "sha256:6eb1454d2931784b2087b62d1fb96d761dd856e026c86103f305be0340ca3492"

[[benchmarks]]
name = "two_spirals_nsga2_mean_fitness"
entry_point = "two_spirals_nsga2_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 10
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/two_spirals_nsga2"
corpus_hash = "sha256:b347ea4844c0d83e575ed0cc664bada8b90d210853c83f680480948ebce568ba"

# ---------------------------------------------------------------------------
# Phase B control benchmarks: GraphGenome + EpisodicEvaluator.
# Added 2026-04-21.
# ---------------------------------------------------------------------------

[[benchmarks]]
name = "cartpole_mean_fitness"
entry_point = "cartpole_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 5
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/cartpole"
corpus_hash = "sha256:2f2b2ddc9943dcb798673f62af16e25933564a249dbe315cc07b3cf73ce45f1d"

[[benchmarks]]
name = "double_pole_mean_fitness"
entry_point = "double_pole_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 60
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/double_pole"
corpus_hash = "sha256:abf4d9390abd0fb7ea93feac803fc2fc87be3851235c963ae6fe839d1348e221"

[[benchmarks]]
name = "mountain_car_mean_fitness"
entry_point = "mountain_car_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 8
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/mountain_car"
corpus_hash = "sha256:855890894c520950f58b579059763440ce271175052ea43cdb3c7947727044ef"

# ---------------------------------------------------------------------------
# Phase D benchmarks: modularity, dynamics, multi-objective showcases.
# Added 2026-04-21.
# ---------------------------------------------------------------------------

[[benchmarks]]
name = "retina_mean_fitness"
entry_point = "retina_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 15
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/retina"
corpus_hash = "sha256:2c3933e824e08cbdabb1210b5e0e0019efcfbaadcdc4e7c3ecda5e6748cd0d8b"

[[benchmarks]]
name = "mackey_glass_mean_fitness"
entry_point = "mackey_glass_mean_fitness"
tier = "quality"
deterministic = false
metric_direction = "minimize"

expected_runtime_seconds = 30
threads = 8
gpu = "none"
background_required = false

repetitions = 5
baseline_seeds = [1, 2, 3, 4, 5]
promotion_sigma = 2.0

corpus_path = "bench/corpus/mackey_glass"
corpus_hash = "sha256:05e0898eee75495625b5db150c6da98dbc9f94872392ef9ba206777e2f7e05c9"
