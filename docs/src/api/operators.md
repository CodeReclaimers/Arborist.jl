# Operators — API Reference

Autogenerated reference for every exported mutation / crossover / selection
operator — including the LLM mutation operator and its prompt-enrichment
sections.

## Types

```@autodocs
Modules = [Arborist]
Pages   = [
    "operators/mutation.jl",
    "operators/crossover.jl",
    "operators/selection.jl",
    "operators/neat_mutation.jl",
    "llm_operator.jl",
    "prompt_context.jl",
]
Order = [:type]
Filter = t -> !startswith(string(nameof(t)), "_")
```

## Functions

```@autodocs
Modules = [Arborist]
Pages   = [
    "operators/mutation.jl",
    "operators/crossover.jl",
    "operators/selection.jl",
    "operators/neat_mutation.jl",
    "llm_operator.jl",
    "prompt_context.jl",
]
Order = [:function]
Filter = t -> !startswith(string(nameof(t)), "_")
```

## Constants

```@autodocs
Modules = [Arborist]
Pages   = [
    "operators/mutation.jl",
    "operators/crossover.jl",
    "operators/selection.jl",
    "operators/neat_mutation.jl",
    "llm_operator.jl",
    "prompt_context.jl",
]
Order = [:constant]
Filter = t -> !startswith(string(t), "_")
```
